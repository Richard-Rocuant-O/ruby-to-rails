<h1>Nuevo Comentario para <%= @article.title %></h1>
<%= form_with(model: [@article, @comment], local: true) do |form| %>

    <div>
        <%= form.label :body %>
        <%= form.text_area :body %>
    </div>

    <div>
        <%= form.submit "Agregar" %>
    </div>
<% end %>
